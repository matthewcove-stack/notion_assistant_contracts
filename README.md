# notion_assistant_contracts

Versioned JSON Schemas shared across the intent normaliser, context API, and Notion execution kernel.

- Schemas are under `schemas/v1/`.
- Examples are under `examples/`.
- The `VERSION` file is the contract version tag expected by services.
