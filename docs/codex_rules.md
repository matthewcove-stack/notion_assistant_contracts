# Codex Rules — notion_assistant_contracts

- Backwards compatibility is preferred. Treat breaking changes as exceptional.
- Every schema change must include:
  - updated example(s)
  - validation passing
  - version bump
- Avoid over-general schemas; keep them minimal and explicit.
